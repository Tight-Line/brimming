<% content_for(:title) { "Edit Answer - Brimming" } %>

<div class="page-header">
  <h1>Edit Answer</h1>
  <p class="text-muted">
    For question: <%= link_to @question.title, question_path(@question) %>
  </p>
</div>

<%= render "form", question: @question, answer: @answer %>
