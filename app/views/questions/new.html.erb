<% content_for(:title) { "Ask a Question - Brimming" } %>

<div class="page-header">
  <h1>Ask a Question</h1>
</div>

<div class="card">
  <%= render "form", question: @question, spaces: @spaces %>
</div>
