<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Brimming" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Brimming">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="site-header">
      <div class="container">
        <%= link_to root_path, class: "site-logo" do %>
          <img src="/icon.svg" alt="Brimming">
          Brimming
        <% end %>

        <nav class="site-nav">
          <%= link_to "Questions", questions_path, class: current_page?(questions_path) ? "active" : "" %>
          <%= link_to "Categories", categories_path, class: current_page?(categories_path) ? "active" : "" %>
        </nav>

        <div class="user-menu">
          <% if user_signed_in? %>
            <span class="user-greeting">
              <%= render 'shared/user_badge', user: current_user %>
            </span>
            <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "btn-logout" %>
          <% else %>
            <%= link_to "Log in", new_user_session_path, class: "btn-login" %>
            <%= link_to "Sign up", new_user_registration_path, class: "btn-signup" %>
          <% end %>
        </div>
      </div>
    </header>

    <main class="main-content">
      <div class="container">
        <% if notice.present? %>
          <div class="flash flash-notice"><%= notice %></div>
        <% end %>
        <% if alert.present? %>
          <div class="flash flash-alert"><%= alert %></div>
        <% end %>
        <%= yield %>
      </div>
    </main>

    <footer class="site-footer">
      <div class="container">
        <p>Brimming &mdash; A Q&amp;A platform by <a href="https://www.tightlinesoftware.com">Tight Line LLC</a></p>
      </div>
    </footer>
  </body>
</html>
